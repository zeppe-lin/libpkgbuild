# Description: parity fixture for manual-page normalization
name=parity-manpages
version=1.0
release=1
source=tool.1

build() {
    mkdir -p "$PKG/usr/share/man/man1"
    install -m 0644 "$SRC/tool.1" "$PKG/usr/share/man/man1/tool.1"
    ln -s tool.1 "$PKG/usr/share/man/man1/tool-alias.1"
}
