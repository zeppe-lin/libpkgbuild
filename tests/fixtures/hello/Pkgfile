# Description: libpkgbuild vertical-slice fixture

name=hello
version=1.0
release=1
source=message.txt

build() {
	mkdir -p "$PKG/usr/share/hello"
	cp "$SRC/message.txt" "$PKG/usr/share/hello/message.txt"
}
