# Description: libpkgbuild download and extraction fixture

name=hello-archive
version=1.0
release=1
source="payload.tar.gz::file://$PKGMK_ROOT/upstream.tar.gz"

build() {
	mkdir -p "$PKG/usr/share/hello-archive"
	cp "$SRC/payload/message.txt" \
		"$PKG/usr/share/hello-archive/message.txt"
}
